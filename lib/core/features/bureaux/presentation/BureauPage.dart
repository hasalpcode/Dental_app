import 'package:flutter/material.dart';

class BureauPage extends StatelessWidget {
  const BureauPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text("Bureau"));
}
