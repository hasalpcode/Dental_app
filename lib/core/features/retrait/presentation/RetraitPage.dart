import 'package:flutter/material.dart';

class RetraitPage extends StatelessWidget {
  const RetraitPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text("Retrait"));
}
